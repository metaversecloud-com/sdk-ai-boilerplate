apiVersion: v1
kind: Service
metadata:
  name: ai-boilerplate0
  labels:
    app.kubernetes.io/name: ai-boilerplate0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-boilerplate0
  ports:
    - name: http
      port: 80
      targetPort: http
